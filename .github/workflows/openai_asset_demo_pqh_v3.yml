name: OpenAI Asset ManimCE PQH Demo V3

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  render:
    if: github.event.pull_request.head.ref == 'agent/openai-asset-manim-pqh-demo-20260815'
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
      - name: Run exact-source audited pipeline
        shell: bash
        run: bash render_jobs/openai_asset_demo_20260815/run_render_v3.sh
      - name: Upload verified delivery
        uses: actions/upload-artifact@v4
        with:
          name: OpenAIAssetInclinedPlaneLesson-PQH-V3
          path: delivery/
          if-no-files-found: error
          retention-days: 7
