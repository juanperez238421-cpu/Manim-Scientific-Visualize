name: Bootstrap Detailed IQR Native PQH Render

# Completed and retained only temporarily until the associated draft PR is closed.
on:
  workflow_dispatch:

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo disabled
