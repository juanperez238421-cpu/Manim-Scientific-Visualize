# Visual HTML Basics V6

Senior-reviewed rebuild of the Visual HTML Fundamentals masterclass.

Scene: `VisualHTMLBasicsV6`

Protocol:

```bash
python -m py_compile visual_html_basics_v6.py
manim -pql visual_html_basics_v6.py VisualHTMLBasicsV6 --format=mp4 --disable_caching
manim -pqh visual_html_basics_v6.py VisualHTMLBasicsV6 --format=mp4 --disable_caching
```

The source is stored as two compressed text parts so the exact render source can be reconstructed reproducibly inside GitHub Actions.
